<%= render 'breadcrumbs' %>

<section class="ds s-pt-60 s-pb-75 s-pb-md-100 s-pb-lg-75 s-py-lg-50 container-px-0">
  <div class="container">
    <div class="row">

      <div class="d-none d-lg-block divider-25"></div>

      <div class="col-lg-12">
        <div class="row justify-content-center">
          <div class="col-md-10 col-xl-8">
            <div class="filters gallery-filters small-text text-lg-right">
              <a href="#" data-filter="*" class="active selected">Portfolio</a>
              <a href="#" data-filter=".carpinteria">aluminio y cristalería</a>
              <a href="#" data-filter=".estructuras">estructuras metálicas</a>
              <a href="#" data-filter=".puertas">puertas</a>
              <a href="#" data-filter=".muebles">muebles</a>
              <a href="#" data-filter=".lumon">cristal de LUMON</a>
              <a href="#" data-filter=".barandillas">acero inoxidable</a>
            </div>
          </div>
        </div>

        <div class="row isotope-wrapper masonry-layout c-mb-30" data-filters=".gallery-filters">

          <%= render 'images-carpinteria' %>
          <%= render 'images-estructuras' %>
          <%= render 'images-puertas' %>
          <%= render 'images-muebles' %>
          <%= render 'images-lumon' %>
          <%= render 'images-acero-inox' %>

        </div>
        <!-- .isotope-wrapper-->

      </div>

      <div class="d-none d-lg-block divider-70"></div>
    </div>

  </div>
</section>
